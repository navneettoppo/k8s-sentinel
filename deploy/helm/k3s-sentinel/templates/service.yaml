apiVersion: v1
kind: Service
metadata:
  name: {{ include "k3s-sentinel.fullname" . }}
  labels:
    {{- include "k3s-sentinel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.service.port | default 80 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k3s-sentinel.selectorLabels" . | nindent 4 }}
